{
  "name": "scripts",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "forge build",
    "deploy": "every set deploy src/DiceSet1155.sol:DiceSet1155 ${SET_REG?} ${KIND_ID?} ${KIND_REV?} --json register/contract.json ",
    "register": "every set register $(every pick contract.addr matter.hash=elements/data.json) --json register/set.json",
    "upload": "every matter register elements/data.json elements/picture.png --json register/matter.json",
    "auto": "run-s 'upload -- {@}' 'deploy -- {@}' 'register -- {@}' --",
    "predeploy": "run-s build"
  },
  "dependencies": {
    "npm-run-all2": "^8.0.4"
  }
}
