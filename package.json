{
  "name": "scripts",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "deploy:set": "forge create src/DiceSet1155.sol:DiceSet1155 --json --constructor-args ${SET_REG?} ${KIND_ID?} ${KIND_REV?} > register/contract.json",
    "register:set": "every set register $(every pick contract.addr matter.hash) --json register/set.json",
    "register:matter": "every matter register elements/data.json --json register/matter.json",
    "predeploy:set": "forge install && forge build && mkdir -p register"
  }
}
